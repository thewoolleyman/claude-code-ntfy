{
  "description": "Send an ntfy push when Claude Code's top-level agent is waiting for the human — a genuine resting point (Stop with no background work, or an idle prompt) or an explicit ask (permission / elicitation). Sub-agent stops and mid-orchestration churn are filtered out by the handler.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks-handlers/notify-ready.sh",
            "timeout": 5
          }
        ]
      }
    ],
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks-handlers/notify-ready.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
